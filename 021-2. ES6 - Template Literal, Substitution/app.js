let s = `This is template literals. We can use ' and " in here.`;
// Template literals
// 백틱(`) 문자를 사용한다

console.log(`Content = ${s}`);
// Template Substitution

console.log(`Upper = ${s.toUpperCase()}`);
